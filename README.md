hello everyone , this is is a simple digital clock  project . 
it is built using HTML,CSS and javaScript(DOM)
it displays the current time and updates automatically evvery seccond using setInterval() setTimeout() function.


features

1   real-time digital clck display .
2    update every second automaticaly .
3    built using bassic web technologies
4    Clean and simple UI

how it is works

The clock uses JavaScripts setTimeout() and setInterval() function to update the time every 100 minlisecond (1 second).
it petches the current time using the date() object and displays it on the screen.
